# Thrift Settings — Workflow

Part of [Thrift](../README.md). Overview: [../workflow.md](../workflow.md).

Schema: [schema.md](./schema.md)

---

## Workflow

1. Open thrift settings for the tenant.
2. Set defaults: origin unit price, hand-tag / sticker unit costs, marketing tag layout.
3. Set `return_window_days` (customer RETURN window; `0` = no customer returns).

**API:** [api/thrift_settings_api.md](./api/thrift_settings_api.md)

Currencies: [`global_reference`](../../global_reference/schema.md).
