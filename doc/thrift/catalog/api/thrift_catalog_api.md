# API: thrift catalog

Tables: `thrift_categories`, `thrift_types`, `thrift_shelves`.

Direct PostgREST CRUD via Supabase client (tenant RLS).

Schema: [../schema.md](../schema.md) · Workflow: [../workflow.md](../workflow.md).
