# Invoice / product profit report (report 4)

Sell vs cost after returns at invoice issue snapshot (`sales_invoice_items.unit_cost_price`).

**RPC:** `get_tenant_invoice_profit_report`  
**Route:** `/:tenantSlug?/app/finance/reports/invoice-profit`

Optional `p_invoice_id` returns SKU line breakdown for the detail dialog.
